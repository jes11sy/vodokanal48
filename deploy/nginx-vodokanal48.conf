# Добавьте в server { } блок сайта водоканал48.рф (как на vodokonal64 и russvodokanal):
#
#   location = /tg-notify {
#       proxy_pass http://127.0.0.1:3377;
#       proxy_http_version 1.1;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#       proxy_set_header X-Forwarded-Proto $scheme;
#       proxy_connect_timeout 15s;
#       proxy_send_timeout 90s;
#       proxy_read_timeout 90s;
#       client_max_body_size 256k;
#   }
#
# tg-proxy должен быть запущен (как на rusvodokanal):
#   systemctl status tg-proxy
#   # или: cd /var/www/rusvodokanal && node tg-proxy.js
#
# Проверка после reload nginx:
#   curl -X POST https://водоканал48.рф/tg-notify \
#     -H "Content-Type: application/json" \
#     -d '{"message":"test"}'
